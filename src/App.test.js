import React from "react";
import { render } from "@testing-library/react";
import App from "./App";

test("Fake test", () => {
  expect(true).toBeTruthy();
});

// it("should render /login without manipulation", () => {
//    const {getByTestId} = render(<App />)
// })
